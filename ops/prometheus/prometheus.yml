global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: signal-room
    metrics_path: /metrics
    static_configs:
      - targets: [app:8080]
        labels:
          service: checkout-api
          environment: local
